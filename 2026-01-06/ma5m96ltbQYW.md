diff --git a/openai-code-review-sdk/src/main/java/com/lss/middleware/sdk/OPenAICodeReview.java b/openai-code-review-sdk/src/main/java/com/lss/middleware/sdk/OPenAICodeReview.javaindex ab704cc..e8f48cf 100644--- a/openai-code-review-sdk/src/main/java/com/lss/middleware/sdk/OPenAICodeReview.java+++ b/openai-code-review-sdk/src/main/java/com/lss/middleware/sdk/OPenAICodeReview.java@@ -62,7 +62,7 @@ public class OPenAICodeReview {          // 创建聊天完成请求         ChatCompletionCreateParams request = ChatCompletionCreateParams.builder()-                .model("glm-4.7")+                .model("GLM-4")                 .messages(Arrays.asList(                         ChatMessage.builder()                                 .role(ChatMessageRole.SYSTEM.value())@@ -73,14 +73,15 @@ public class OPenAICodeReview {                 ))                 .stream(false)                 .temperature(0.6f)-                .maxTokens(1024)                 .build();          // 发送请求         ChatCompletionResponse response = client.chat().createChatCompletion(request);-         // 获取回复-        return response.getData().getChoices().get(0).getMessage().getContent().toString();+        String review = response.getData().getChoices().get(0).getMessage().getContent().toString();++        client.close();+        return review;     }  